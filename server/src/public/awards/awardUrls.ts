import keys from "../../config/keys";

const awardUrls = [
  `${keys.serverUrl}/api/awards/gold-medal`,
  `${keys.serverUrl}/api/awards/heart-award.svg`,
  `${keys.serverUrl}/api/awards/medal-silver.svg`,
  `${keys.serverUrl}/api/awards/sparkles.svg`,
];

export default awardUrls;
